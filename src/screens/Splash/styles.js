import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  splashContainer: {
    display: 'flex',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  loadingText: {
    marginTop: 20,
    textAlign: 'center',
    fontWeight: '300',
  },
});
