import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  menuWrapper: {
    position: 'absolute',
    top: 0,
    right: 0,
  },
  iconContextMenu: {
    position: 'absolute',
    top: 0,
    right: 10,
  },
  optionStyle: {
    padding: 10,
  },
});
