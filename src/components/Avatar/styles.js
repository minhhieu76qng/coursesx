import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    // width: 70,
    alignItems: 'center',
    paddingHorizontal: 10,
  },
  avatarName: {
    marginTop: 10,
  },
});
