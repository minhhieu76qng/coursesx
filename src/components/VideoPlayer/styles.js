const { StyleSheet } = require('react-native');

export default StyleSheet.create({
  container: {
    // flex: 1,
    width: '100%',
  },
  otherComps: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
});
